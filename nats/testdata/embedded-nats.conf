# Set port to a random free port
port: -1

jetstream {
  max_mem: 2G
  max_file: 100G
  # store_dir is configured in code, but can be overridden by setting it here
  # store_dir: /user/local/data/jetstream
}
